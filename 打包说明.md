# BrickStudio 打包指南(电脑版 + 安卓版)

本次文件已直接写入项目目录。先装一次新依赖:

```
npm install
```

(会新装 electron 和 @electron/packager;electron 首次安装要下载约 100MB 二进制)

---

## 电脑版(Electron + Inno Setup)

一键:双击 **`打包电脑版.bat`**(或 `npm run pack:win`),流程 = 前端构建 → electron-packager 出绿色目录 → 调用 `iscc` 出安装包。

产物:
- 安装包:`release\BrickStudio-Setup-0.8.0.exe`(桌面快捷方式、开始菜单、卸载项)
- 绿色免安装版:`release\BrickStudio-win32-x64\BrickStudio.exe`

说明:
- 应用内嵌了原本地服务(同一套代码),启动即用,无需 Node
- 模型库自动保存在 `文档\BrickStudio模型\`(标准 .ldr,Studio 可直接打开)
- 导出的 .ldr / CSV / XLSX / 图片自动落到系统「下载」文件夹并弹出定位
- 开发调试可 `npm run electron` 直接跑不打包

## 安卓版(WebView 壳,零 Capacitor 依赖)

一键:双击 **`打包安卓版.bat`**(或 `npm run pack:android`)。

前置:
- JDK 17+(`java -version` 或设 JAVA_HOME)
- Android SDK(脚本按 ANDROID_HOME → ANDROID_SDK_ROOT → `%LOCALAPPDATA%\Android\Sdk` 顺序找)
- SDK licenses 已接受:`sdkmanager --licenses` 全部 y 一遍
- 无需安装 gradle:脚本会自动下载 gradle-8.7 到 `android\.gradle-dist\` 并解压使用(约 120MB,一次性);网络不便也可手动下载 `gradle-8.7-bin.zip` 放到该目录

产物:项目根目录 **`BrickStudio.apk`**(release 构建、debug 签名,可直接传手机安装;不可上架商店)。

安卓版特性:
- 完全离线(零权限,零件库打进 APK 的 assets,约 50MB)
- 模型自动保存用 WebView 本地存储;导出 .ldr / 零件表会写入手机「下载」目录并弹提示
- 应用内「打开文件」走系统文件选择器

## 常见问题

- `iscc 不是内部命令`:确认 Inno Setup 6 安装目录在 PATH,或手动用 Inno Setup 打开 `build\installer.iss` 编译
- 安卓构建报 license 错:跑 `sdkmanager --licenses`
- 安卓构建下载依赖慢:AGP/依赖从 google()/mavenCentral 拉取,第一次较久,之后有缓存
- Electron 安装失败:公司代理环境可设 `ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/` 再 `npm install`
- 两个 bat 都会 `pause`,报错信息不会一闪而过

## 安装包分发(局域网下载)

打包完的 APK/EXE 需要传到手机上测试?双击 **`分发安装包.bat`**，它会在本机启动一个局域网下载页。

等效命令行:

```
node tools\dist-server.ts
```

(Node 22.6~23.5 需要加 `--experimental-strip-types`；Node 23.6+ 原生支持。脚本会自动 fallback 到 esbuild 编译后运行 `.mjs`。)

手机和电脑连同一个 WiFi，用手机浏览器打开控制台打印的局域网地址(如 `http://192.168.x.x:8088`)，即可下载 APK 直接安装。默认端口 8088，被占用会自动 +1。

特性:
- 自动发现项目根目录的 `.apk` 和 `release\` 下的 `.exe`/`.zip`，换版本号不用改代码
- 支持断点续传(Range)，大文件下载不怕断
- 仅暴露白名单文件，不会泄露项目源码
- `/list.json` 提供机器可读的文件清单，方便脚本化取包

## 版本升级

改版本号的位置:`package.json` 的 version、`tools\win-build.mjs` 的 appVersion、`build\installer.iss` 的 MyAppVersion、`android\app\build.gradle` 的 versionCode/versionName。
