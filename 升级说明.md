# BrickStudio v7 升级包

适用于:G:\C\LDD-android\brickstudio-project\brickstudio

## 覆盖方法
1. `src\` 整个覆盖(全量替换)
2. `tools\serve.mjs`、`tools\dev.mjs`、`tools\pack-parts.mjs` 覆盖
3. `public\sw.js` 覆盖(缓存版本已升级,更新后刷新两次生效)
4. 重新构建启动:`npm run dev`

## 新功能
- **👣 步骤预览**(菜单):滑条逐步查看搭建过程;「导出 .ldr」现在自动按层插入 `0 STEP`,BrickLink Studio / LeoCAD 打开直接显示搭建步骤;打开含 STEP 的文件也按原步骤预览
- **⚡ 高性能模式**(菜单):一键关闭阴影与边线,大模型流畅度大幅提升,设置会记住
- **快捷视角**(菜单):等轴 / 顶 / 前 / 侧
- 修复:加载模型途中点「新建」可能残留零件的问题;自动保存恢复不会再覆盖已开始的搭建

## 全量零件库(可选,约 6800+ 种)
你项目根目录已有解压好的 `complete\` 文件夹,想要人仔/动物/火车/船/电子件/全套科技件的话,在项目目录执行:

```
node tools\pack-parts.mjs complete\ldraw --full
npm run dev
```

约 +160MB 磁盘(零件按需加载,不占内存)。零件面板会自动多出「人仔 / 动物 / 火车船 / 电子」页签。想回到标准档就去掉 `--full` 重跑。
注意:--full 后不要构建单文件版(会把 200MB 塞进一个 html)。
